<h1>Listing picoposts</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th>When</th>
	<th></th>
    <th></th>
    <th></th>
  </tr>

<% @picoposts.each do |picopost| %>
  <tr>
    <td><%= picopost.content %></td>
    <td><%= picopost.user_id %></td>
    <td><%= picopost.updated_at %></td>
    
    <td><%= link_to 'Show', picopost %></td>
    <td><%= link_to 'Edit', edit_picopost_path(picopost) %></td>
    <td><%= link_to 'Destroy', picopost, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Picopost', new_picopost_path %>
